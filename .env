# Server
PORT=7001
NODE_ENV=development

# Database (MySQL)
# DB_HOST=localhost
# in case of docker db host name will be like below
DB_HOST=host.docker.internal
DB_USER=root
DB_PASSWORD=password
DB_NAME=user_db
DB_PORT=3306